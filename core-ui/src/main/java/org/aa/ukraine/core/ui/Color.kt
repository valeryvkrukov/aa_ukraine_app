package org.aa.ukraine.core.ui

import androidx.compose.ui.graphics.Color

// --- LIGHT THEME ---
val AAPrimaryBlue = Color(0xFF1D70B8)    // Accents, buttons
val AASecondaryBlue = Color(0xFF004F9F)  // Headers, Toolbars
val AABackground = Color(0xFFFAFAFA)     // Soft screen background
val AASurface = Color(0xFFFFFFFF)        // Group cards
val AAOnSurfaceText = Color(0xFF2C3E50)  // Readable schedule text
val AAMutedText = Color(0xFF7F8C8D)      // Time, secondary data
val AAAmberAccent = Color(0xFFF39C12)    // Warm amber / golden

// --- DARK THEME ---
val AAPrimaryBlueDark = Color(0xFF6CACE4)     // A lighter blue for contrast in the dark
val AASecondaryBlueDark = Color(0xFF4A90E2)   // Secondary blue accent
val AAAmberAccentDark = Color(0xFFF5B041)     // Muted amber
val AABackgroundDark = Color(0xFF121212)      // Classic deep dark background
val AASurfaceDark = Color(0xFF1E1E1E)         // A lighter shade of black for cards
val AAOnSurfaceTextDark = Color(0xFFE0E0E0)   // Soft white text (doesn't dazzle the eyes)
val AAMutedTextDark = Color(0xFF9E9E9E)       // Gray text for secondary data

// --- SPECIFIC COLORS ---
val AASiteHeaderBackground = Color(0xFFF4F0EE)  // Background color of official site header
val AALogoBackground = Color(0xFF085080)        // Background color of official logo